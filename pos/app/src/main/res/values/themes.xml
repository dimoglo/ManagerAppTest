<resources>
    <!-- Base application theme. -->
    <style name="Theme.Nomia" parent="Theme.AppCompat.DayNight.NoActionBar" />

    <style name="Theme.Nomia.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_inset</item>
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="postSplashScreenTheme">@style/Theme.Nomia</item>
    </style>
</resources>
